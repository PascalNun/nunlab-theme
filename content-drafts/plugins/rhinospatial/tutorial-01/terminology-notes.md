# Terminology Notes

Canonical names checked against the local RhinoSpatial README/docs:

- RhinoSpatial
- Rhino
- Grasshopper
- Package Manager
- Components folder
- Spatial Context
- Open Map
- Use Absolute Coordinates
- Reference URL
- Load WFS
- List WFS Layers
- WFS URL
- Load WMS
- List WMS Layers
- Load GeoTIFF
- EPSG
- Load Terrain
- WCS URL
- DEM
- Load LoD2 Buildings
- LoD2
- CityGML
- CityJSON
- ZIP archive
- Load OSM
- OpenStreetMap
- Overpass API
- 3D Tiles Viewer (Google)
- Google Photorealistic 3D Tiles
- Map Tiles API key

Notable Whisper corrections:

- `Rhino Spatial` -> `RhinoSpatial`
- `grasshopper` -> `Grasshopper`
- `CTGML` -> `CityGML`
- `CTJSON` -> `CityJSON`
- `ACIP folder` -> `ZIP archive or folder`
- `Google 3D tiles component` -> `3D Tiles Viewer (Google)` where referring to the component name
- `mesh rendering alias` -> `mesh rendering artifacts`
- `Maps tiles API key` -> `Map Tiles API key`

Open wording checks:

- The transcript says WMS can only load one layer per component. Keep this if it matches the current component behavior.
- The transcript says buildings are loaded by default in Load OSM. Keep this if it matches the current default inputs.
- The transcript says the 3D Tiles viewer can display but not bake/save Google data. Keep this language concise in captions, and use a fuller policy note in the YouTube description or website copy if needed.
